[build]
dockerfilePath = "Dockerfile.railway"

[deploy]
# Railway inyecta PORT; el CMD del Dockerfile lo lee via $PORT
# Sin healthcheck path para evitar falsos negativos durante el startup largo
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
